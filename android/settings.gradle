dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven { url 'https://nexus3.hzcxfw.com/repository/andriod-group/' }
        google()
        mavenCentral()
    }
}

rootProject.name = 'hzlogger'